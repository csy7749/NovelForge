## 1. OpenSpec 对齐

- [x] 1.1 确认 `context-visualization` capability 的 proposal、design、spec 内容与当前上下文链路一致
- [x] 1.2 使用 OpenSpec 校验 `add-context-visualization` 变更文档

## 2. 后端上下文 trace 扩展

- [x] 2.1 梳理 `assemble_context` 当前输出与前端可视化所需字段的差距
- [x] 2.2 为上下文响应模型新增可选 trace 元数据结构，保持现有字段兼容
- [x] 2.3 在事实子图、关系摘要、物品摘要、概念摘要等装配路径中填充来源明细与截断标记
- [x] 2.4 为上下文装配失败或缺失来源提供显式错误/空状态输出，不添加静默 fallback

## 3. 前端可视化模型接入

- [x] 3.1 扩展 `frontend/src/renderer/src/api/ai.ts` 的上下文响应类型，接入新增 trace 字段
- [x] 3.2 梳理 `contextResolver` 当前支持的 token 类型，并确定模板视图需要展示的最小结构
- [x] 3.3 提炼上下文可视化所需的前端视图模型，避免在组件内重复拼装业务规则

## 4. ContextDrawer 可视化实现

- [x] 4.1 在现有上下文抽屉中新增“模板视图 / 解析视图 / 最终注入视图”切换
- [x] 4.2 实现模板 token 的高亮展示和基础元信息呈现
- [x] 4.3 实现解析视图的来源列表，展示来源类别、来源对象、预览内容、命中数量和截断状态
- [x] 4.4 实现最终注入视图，确保显示模型实际可见的上下文文本
- [x] 4.5 为未解析 token、空模板、装配失败增加明确 UI 状态

## 5. 编辑器集成与验证

- [x] 5.1 在 `GenericCardEditor` 中联通生成与审核场景的上下文可视化入口
- [ ] 5.2 手工验证卡片引用、`facts.*`、`kg:*`、`$self/$parent/$current` 等典型来源的显示效果
- [ ] 5.3 验证上下文缺失、未命中、截断和装配失败场景的暴露行为
- [x] 5.4 运行相关前端类型检查与必要回归，确认未破坏现有上下文抽屉和生成流程
