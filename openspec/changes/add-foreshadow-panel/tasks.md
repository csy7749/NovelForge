## 1. OpenSpec 对齐

- [ ] 1.1 确认 `foreshadow-panel` capability 的 proposal、design、spec 内容与当前仓库结构一致
- [ ] 1.2 使用 `openspec validate add-foreshadow-panel` 校验变更文档

## 2. 前端接入准备

- [ ] 2.1 复用现有 `frontend/src/renderer/src/api/ai.ts` 伏笔接口，补齐面板所需的请求参数和返回处理
- [ ] 2.2 梳理当前激活卡片、项目 id、章节上下文在编辑器中的可用来源

## 3. 伏笔面板实现

- [ ] 3.1 新增 `ForeshadowPanel.vue`，实现项目级伏笔列表、状态筛选、刷新和空状态
- [ ] 3.2 在面板中实现手动登记表单，支持 `title/type/note/chapter_id`
- [ ] 3.3 在章节正文上下文中实现候选建议读取与勾选登记
- [ ] 3.4 在列表项上实现“标记已回收”、“删除”、“跳转到章节”操作

## 4. 编辑器集成

- [ ] 4.1 在 `Editor.vue` 右侧 Tab 中新增 `伏笔` 面板挂载
- [ ] 4.2 调整章节卡片与非章节卡片下的 Tab 可见性和默认行为
- [ ] 4.3 确保跳转章节时复用现有卡片激活与导航逻辑，不复制新导航通道

## 5. 验证

- [ ] 5.1 手工验证列表、登记、候选建议、回收、删除、跳转的完整路径
- [ ] 5.2 运行前端类型检查与必要的交互回归，确认未破坏现有右侧侧栏体验
